{
  "name": "animation-dev-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The render + verify workbench for the MotionLoom pipeline. The static build in dist/ shows scenes; scripts/ render deterministic snapshots for PR review.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "snapshot": "node scripts/snapshot.mjs",
    "security:smoke": "node scripts/security-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  },
  "license": "MIT"
}
